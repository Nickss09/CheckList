import React from 'react';
import './List.css';

const List = ({ id, label }) => {
    return (
        <div className="digitar">
            <div className=''> 
            
            </div>
        </div>
    );
};

export default List;