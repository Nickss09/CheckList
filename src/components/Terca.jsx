import React from 'react';
import './List.css';

const List = ({ id, label }) => {
    return (
        <div className="sexta">
            <div className='item'> 
            
            </div>
        </div>
    );
};

export default List;